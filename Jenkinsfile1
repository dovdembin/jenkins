@Library('my-shared-library') _
pipeline {
    agent any
    triggers { pollSCM('* * * * *') }
    stages {
            libOtel.getLabels("asdf")
        stage('Build') {
            
            steps {
                sh "ls -l"
            }
        }
    }
    post {
        always {
            archiveArtifacts artifacts: '*.md'
        }
    }
    
}
